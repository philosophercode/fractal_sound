{
  "name": "fractal-sound",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && cp dist/index.html . && cp dist/assets/* . && sed -i '' 's|\"/fractal-sound/assets/|\"/fractal-sound/|g' index.html"
  },
  "dependencies": {
    "p5": "^1.9.0"
  },
  "devDependencies": {
    "@types/node": "^20.17.10",
    "@types/p5": "^1.7.6",
    "typescript": "^5.3.3",
    "vite": "^5.1.4"
  }
}